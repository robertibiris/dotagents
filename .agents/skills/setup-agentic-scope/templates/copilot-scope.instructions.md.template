---
applyTo: "{{SCOPE_PATH}}/**/*"
---

<!-- Generated by setup-agentic-scope. Canonical source: {{SCOPE_PATH}}/AGENTS.md. -->
Use `{{SCOPE_PATH}}/AGENTS.md` as the canonical scope map for files under `{{SCOPE_PATH}}/`. Resolve registered paths from that map's directory and load registered resource bodies only when their frontmatter description is relevant.
