interface:
  display_name: "Setup Agentic Scope"
  short_description: "Create and compose independent agent scopes"
  default_prompt: "Use $setup-agentic-scope to create an independent scope or compose one-way child routes with routing hints."
